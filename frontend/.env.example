# Backend API URL
# WebSocket URL is automatically derived from this:
# - Strips '/api' suffix, converts http:// to ws:// (https:// to wss://)
# - Example: http://172.18.220.56:8008/api -> ws://172.18.220.56:8008
# For reverse-proxy deployment (recommended), keep this as /api so the frontend
# works with either IP-based or DNS-based hostnames without rebuild changes.
VITE_API_BASE_URL=/api

# External Authentication URL 
VITE_EXTERNAL_AUTH_URL=http://172.18.220.56:9001/

# Enable external auth redirect (set to 'false' for local development)
VITE_ENABLE_EXTERNAL_AUTH=false

# Application Environment
VITE_APP_ENV=staging

# API Timeout (milliseconds)
VITE_API_TIMEOUT=30000